char sql_CreateChallenges[] = "CREATE TABLE IF NOT EXISTS ck_challenges (id INT NOT NULL AUTO_INCREMENT, mapname VARCHAR(32), StartDate TIMESTAMP, EndDate TIMESTAMP, style INT(12) NOT NULL DEFAULT '0', points INT(12) NOT NULL DEFAULT '0', active INT(12) NOT NULL DEFAULT '0', PRIMARY KEY(id)) DEFAULT CHARSET=utf8mb4;";
char sql_CreateChallenges_Times[] = "CREATE TABLE IF NOT EXISTS ck_challenge_times (id INT(12) NOT NULL, steamid VARCHAR(32), name VARCHAR(32), mapname VARCHAR(32), runtime FLOAT NOT NULL DEFAULT '-1.0', style INT(12) NOT NULL DEFAULT '0', Run_Date TIMESTAMP ON UPDATE CURRENT_TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY(id, steamid, mapname, runtime)) DEFAULT CHARSET=utf8mb4;";

//char sql_selectCurrentChallengeLeaderBoard[] = "SELECT steamid, name, mapname, runtime FROM ck_challenge_times WHERE mapname = '%s' AND Run_Date BETWEEN '%s' AND '%s';";
